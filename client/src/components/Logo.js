import React from 'react';
import logo from '../images/logo.jpg';

const Logo = () => (
  // let tolanding = window.location +'/test'

<img src={logo} alt="Logo" />
setTimeout(() => window.location +'/test', 5000);
);

export default Logo;
